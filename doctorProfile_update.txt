Improve the public Doctor Profile page.

Goal:
The doctor profile must become more complete and informative, following the requirements from /docs/logic/pagesToComplete.pdf.

The top profile section must show:

use /docs/mockups/doctorTopProfile.png
- doctor photo
- doctor name
- specialty
- license number
- address/location
- rating
- buttons:
  - Book Appointment
  - Send Message

Add these profile sections:
use /docs/mockups/doctorExperience.png
1. Experience / About
   - description
   - years of experience
   - diseases treated
   - patient types:
     - adults
     - children
     - elderly
   - consultation types:
     - in-person
     - online consultation
     - home visit if available

2. More Information modal
use /docs/mockups/doctorMoreInformation.png
   Add a "Show more information" button.
   When clicked, show a modal/pop card with:
   - experience
   - education
   - certifications
   - diseases treated
   - languages
   - publications/papers
   - awards/distinctions

3. Services and Prices
use /docs/mockups/doctorPrices.png
   Each service must show:
   - service title
   - price
   - duration if available
   - "Book appointment" button

4. Care Locations / Offices
use /docs/mockups/doctorLocations.png
   Show a card with tabs for each care location.
   When user clicks each tab, show:
   - address
   - availability
   - phone number
   - small map preview placeholder
   - button to view route / directions

5. Gallery

   Show doctor/clinic photos in a grid inside a card.

6. Reviews
   use /docs/mockups/doctor_review.png
   Show:
   - patient name
   - comment
   - rating

7. Schedule / availability
copy the ui from /docs/mockups/doctorSchedule.png
   Add a visible schedule section or booking sidebar that shows available days and times.

Important:
- Use mock data only.
- Do not connect real backend.
- Keep the profile route working.
- Keep the current layout if it exists, but improve it.
- If the profile page supports clinic too, do not break clinic profiles.
- Use reusable components.

Expected components:
- ProfileHero.tsx
- DoctorExperienceCard.tsx
- MoreInfoModal.tsx
- ServicesPricingCard.tsx
- CareLocationsTabs.tsx
- GalleryGrid.tsx
- ReviewsSection.tsx
- DoctorAvailabilityPanel.tsx

Mock data:
Extend doctor data with:
- licenseNumber
- diseasesTreated
- patientTypes
- consultationTypes
- education
- certifications
- languages
- publications
- awards
- services
- careLocations
- gallery
- reviews
- schedule

Security:
- Do not render unsafe HTML.
- Treat all displayed text as plain text.
- Validate interaction state.

After implementing:
- Run build/dev validation.
- Fix errors.
- Summarize created components and remaining pending work.
