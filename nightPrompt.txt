Current goal:
Improve the existing prototype without breaking current navigation.

1. Doctor/clinic professional portal 
2. Search map page (update) the informationial card of the doctor need to be update
3. Login page (update) see in /docs/mockups
4. Doctor/Clinic profile page (update) see details /docs/mockups/profesionalProfile it will include the components you need to create 


Context folders:
- docs/logic contains all business rules, MVP rules, data structure, and product documentation.
- docs/mockups contains screen mockups as images and matching .txt files explaining each screen, and the pallete colors you will follow.

Before coding:
1. Read GEMINI.md if it exists.
2. Read everything inside docs/logic.
3. Inspect docs/mockups.
4. For each mockup image, read the matching .txt file.
   Example:
   - home_page.png
   - home_page.txt

Important:
- The user must be able to navigate through the full flow.
- Use mock data only.
- Do not connect to real backend yet.
- Keep API/data access separated so it can later be replaced with real HTTP requests.
- Use React Router for navigation.
- Use TypeScript.
- Use Tailwind CSS.
- Use reusable components.
- UI must follow the visual mockups as closely as possible.
- Use the color palette from docs/logic.
- Use Leaflet or a placeholder map component if Leaflet setup becomes blocking.
- The search map must show doctors/clinics from mock data.
- Selecting a doctor or clinic should redirect to login first.
- After successful mock login, redirect to the selected doctor/clinic profile.
- If no selected profile exists after login, redirect to search map.

Architecture:
Use this final structure:

For architecture I need you to suggest me how can we separate 2 differents parts : Common User and Doctors/clinics, as this project is a prototype, i want to create a monolyth and use one single project for both parts, common users will use the doctor searchs, locate the doctor in the map, and see doctors profiles, doctors/clinics will use an ui with a calendar to visualize the appointments they have. The point this project should sepparate: https://domain/homePage (search for doctors) and https://domain/professional (doctors calendar)

Security / quality:
- Follow OWASP frontend basics.
- No secrets in frontend.
- Do not use dangerouslySetInnerHTML.
- Validate form input.
- Keep components small and reusable.
- Avoid hardcoding business logic inside visual components.
- Do not implement features outside the MVP unless needed for navigation.
- Do not delete documentation files.
- Do not modify docs/logic unless explicitly required.

Expected output:
1. Working frontend project.
2. Navigation flow fully functional.
4. Mock profile data rendered.
5. README section explaining how to run with Docker and local dev.
6. A short summary of what was created and what remains pending.

Work iteratively:
- First create project structure.
- Then implement routing.
- Then mock data.
- Then each page.
- Then polish visual similarity to mockups.
- Then run/build/test.
- Fix errors until the app starts successfully.
